<div>
    <div class="identityHeader">
        <div class="profileImage">
            <img class="profileImage" src="{{profile.profilePicture}}"/>
        </div>
        <div class="profileDetails">
            <h1>{{profile.name}}</h1>
            <p>Contact info:<br/>{{profile.email}}</p>
        </div>
    </div>
</div>